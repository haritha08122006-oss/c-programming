#include<stdio.h>
#include<stdlib.h>
struct roll{
    int number;
    struct roll*next;
};
int main()
{
    struct roll*ct1 = (struct roll*)malloc(sizeof(struct roll));
    struct roll*ct2 = (struct roll*)malloc(sizeof(struct roll));

    
    

 scanf("%d",&ct1->number);

      scanf(" %d",&ct2->number);
      
     
     ct1->next = ct2;
     ct2->next = NULL;
    
     
   
     
     struct roll *head = ct1;
     printf("Contact: \n");
     while(head != NULL){
         printf(" ROLL %d----->",head->number);
         head = head->next;
     }
         printf("NULL");
     
     return 0;
     }