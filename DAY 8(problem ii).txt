#include <stdio.h>
#include<stdlib.h>
struct contact{
     long int number;
     struct contact *next;
};
int main(){
    struct contact *ct1=(struct contact*)malloc(sizeof(struct contact));
    struct contact *ct2=(struct contact*)malloc(sizeof(struct contact));
    struct contact *ct3=(struct contact*)malloc(sizeof(struct contact));
    ct1->number=9080909127;
     ct2->number=7904233361;
      ct3->number=6374260804;
      
      ct1->next=ct2;
      ct2->next=ct3;
      ct3->next=NULL;
struct contact *head=ct1;
printf("contact: \n");
while(head !=NULL){
    printf("%ld---> ",head->number);
    head=head->next;
}
    return 0;
     
}